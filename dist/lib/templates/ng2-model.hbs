/* tslint:disable */
{{#each modelDefinition.refs}}
import { {{ this }} } from './{{this}}';
{{/each}}

{{#each modelDefinition.enums}}
export enum {{@key}} {
  {{#each this}}
    {{ this }}{{#if @last}}{{else}}, {{/if}}
  {{/each}}
}
{{/each}}


export interface {{modelName}}{
{{#each modelDefinition.properties}}
  {{#if isEnum}}
    {{ name~}}?: {{ type~}}{{#if isArray}}[]{{/if}};
  {{ else }}
    {{ name~}}?: {{ type~}}{{#if isArray}}[]{{/if}};
  {{/if}}
{{/each}}
}
